<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:batch="http://www.springframework.org/schema/batch"
       xmlns:p="http://www.springframework.org/schema/p" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans	http://www.springframework.org/schema/beans/spring-beans.xsd
	    http://www.springframework.org/schema/batch 	http://www.springframework.org/schema/batch/spring-batch.xsd
        http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">


    <bean id="orderCreateTask" class="com.isec.sc.intgr.scheduler.OrderCreateTask"/>
    <bean id="orderUpdateTask" class="com.isec.sc.intgr.scheduler.OrderUpdateTask"/>

    <bean name="orderCreateJob_OUTRO" class="org.springframework.scheduling.quartz.JobDetailBean"
          p:jobClass="com.isec.sc.intgr.scheduler.OrderCreateJobBean">
        <property name="jobDataAsMap">
            <map >
                <entry key="orderCreateTask" value-ref="orderCreateTask"/>
                <entry key="redisKey" value="${key.ma.outro.order}" />
                <entry key="redisPushKey" value="${key.ma.outro.orderUpdate.S2M}" />
                <entry key="redisErrKey" value="${key.ma.outro.order.error}" />
            </map>
        </property>
    </bean>
    
    <bean name="orderUpdateJob_OUTRO" class="org.springframework.scheduling.quartz.JobDetailBean"
          p:jobClass="com.isec.sc.intgr.scheduler.OrderUpdateJobBean">
        <property name="jobDataAsMap">
            <map >
                <entry key="orderUpdateTask" value-ref="orderUpdateTask"/>
                <entry key="redisKey" value="${key.ma.outro.order}" />
                <entry key="redisPushKey" value="${key.ma.outro.orderUpdate.S2M}" />
                <entry key="redisErrKey" value="${key.ma.outro.order.error}" />
            </map>
        </property>
    </bean>

    <bean id="orderCreateTrigger_OUTRO" class="org.springframework.scheduling.quartz.CronTriggerBean"
          p:jobDetail-ref="orderCreateJob_OUTRO"
          p:startDelay="1000"
          p:cronExpression="0/10 * * * * ?"/>
          
    <bean id="orderUpdateTrigger_OUTRO" class="org.springframework.scheduling.quartz.CronTriggerBean"
          p:jobDetail-ref="orderCreateJob_OUTRO"
          p:startDelay="1000"
          p:cronExpression="0/5 * * * * ?"/>


    <bean id="schedulerFactoryBean" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="orderCreateTrigger_OUTRO"/>
                <ref bean="orderUpdateTrigger_OUTRO"/>
            </list>
        </property>
    </bean>

</beans>
